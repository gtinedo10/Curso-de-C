#include <stdio.h> 
#include <limits.h>
#include <stdlib.h>// Header: standar de entrada y salida

int main()
{
    printf("INT _MAX %i \n", INT_MAX);
    printf("INT _MIN %d \n", INT_MIN);
    printf("SHORT _MAX %d \n", SHRT_MAX);
    printf("LONG MAX %d \n", LONG_MAX);
    printf("CHAR _BIT %d \n", CHAR_BIT);

    return 0;
}
